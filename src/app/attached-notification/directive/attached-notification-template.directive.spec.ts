import { AttachedNotificationTemplateDirective } from './attached-notification-template.directive';

describe('AttachedNotificationTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachedNotificationTemplateDirective();

    expect(directive).toBeTruthy();
  });
});
